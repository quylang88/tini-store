// Service Worker for PWA
// Required for iOS Web Push / Notifications to work reliably in some contexts
self.addEventListener('fetch', () => {
  // Pass through - we just need the SW to exist and be registered
});

# Palette's Journal

## 2024-05-22 - Missing Navigation Labels
**Learning:** Common navigation components like Calendars often rely solely on icons (chevrons) without accessible labels, making them unusable for screen readers.
**Action:** Always check `onClick` handlers on icon-only buttons and ensure an `aria-label` describes the action (e.g., "Previous Month").
